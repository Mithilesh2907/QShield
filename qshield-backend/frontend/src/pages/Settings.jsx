import PlaceholderPage from '../components/PlaceholderPage';

export default function Settings() {
  return <PlaceholderPage title="System Settings" description="Configure scan parameters, API keys, and user preferences." />;
}
