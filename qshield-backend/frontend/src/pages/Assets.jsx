import PlaceholderPage from '../components/PlaceholderPage';

export default function Assets() {
  return <PlaceholderPage title="Assets Inventory" description="List of all scanned assets, domains, and endpoints." />;
}
